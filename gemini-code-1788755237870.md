# PROJECT_SPEC: Watch-P2P (순수 C2C 시계 직거래 플랫폼)

## 1. 프로젝트 비즈니스 모델 및 운영 정책
- 목표: 플랫폼 마진, 직매입, 유료 검수 없이 사용자 간 직거래(P2P)를 중개하는 시계 전용 마켓플레이스 (과거 플라이워치 중고장터 모델 현대화).
- 법적 지위: 순수 통신판매중개자 (거래 대금 결제 관여 없음, 가품/하자 분쟁 면책, 당사자 간 직거래 기본).
- 2-Track 카테고리 분리 정책:
  1. ENTRY_MID (엔트리~미드/럭셔리): 기준가 1,000만 원 미만 (세이코, 해밀턴, 론진, 튜더, 오메가 등). 택배 및 직거래 병행 가능.
  2. HIGH_END (하이엔드/탑티어): 기준가 1,000만 원 이상 (롤렉스, 까르띠에, AP, 파텍 등). 대면 직거래 원칙 + 당일 날짜/닉네임/지정시각 실물 인증샷 1번 사진 강제.
- 플랫폼 핵심 룰:
  - 도배 방지: 일반 회원은 최근 24시간 내 최대 3개 매물만 등록 가능 (업자 도배 차단).
  - 시세 아카이빙: 거래 완료(SOLD) 매물은 물리 삭제 금지. SOLD OUT 음영 처리 후 영구 보존하여 시세 데이터로 활용.
  - 판매 인증: 판매 매물 등록 시 최초 1회 휴대폰 본인인증 필수.
  - 공지 의무: 전 화면 및 상세 페이지에 통신판매중개자 면책 문구 상시 고정.

---

## 2. 데이터베이스 스키마 (DDL: MariaDB / MySQL)

DROP TABLE IF EXISTS item_images;
DROP TABLE IF EXISTS watch_items;
DROP TABLE IF EXISTS users;

CREATE TABLE users (
    user_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(100) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    nickname VARCHAR(50) NOT NULL UNIQUE,
    phone_number VARCHAR(20) NULL,
    is_phone_verified BOOLEAN DEFAULT FALSE,
    user_role VARCHAR(20) DEFAULT 'MEMBER',
    manner_score INT DEFAULT 36,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE watch_items (
    item_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    seller_id BIGINT NOT NULL,
    category_tier VARCHAR(20) NOT NULL,     -- ENTRY_MID, HIGH_END
    brand VARCHAR(50) NOT NULL,
    model_name VARCHAR(100) NOT NULL,
    ref_number VARCHAR(50) NULL,
    movement_type VARCHAR(20) NOT NULL,     -- AUTOMATIC, MANUAL, QUARTZ
    case_size_mm DECIMAL(4, 1) NULL,
    dial_color VARCHAR(30) NULL,
    stamping_date VARCHAR(7) NULL,          -- YYYY-MM
    origin_type VARCHAR(30) NOT NULL,       -- DOMESTIC_STORE, OVERSEAS, PARALLEL, UNKNOWN
    has_box BOOLEAN DEFAULT FALSE,
    has_guarantee_card BOOLEAN DEFAULT FALSE,
    has_manual BOOLEAN DEFAULT FALSE,
    extra_links_count INT DEFAULT 0,
    price BIGINT NOT NULL,
    trade_type VARCHAR(20) NOT NULL,        -- DIRECT_ONLY, DELIVERY_AVAILABLE
    preferred_location VARCHAR(150) NULL,
    description TEXT NOT NULL,
    item_status VARCHAR(20) DEFAULT 'FOR_SALE', -- FOR_SALE, RESERVED, SOLD
    view_count INT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_category_tier (category_tier),
    INDEX idx_item_status (item_status),
    INDEX idx_created_at (created_at),
    FOREIGN KEY (seller_id) REFERENCES users(user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE item_images (
    image_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    item_id BIGINT NOT NULL,
    image_url VARCHAR(500) NOT NULL,
    is_representative BOOLEAN DEFAULT FALSE,
    is_verification_photo BOOLEAN DEFAULT FALSE,
    sort_order INT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_item_id (item_id),
    FOREIGN KEY (item_id) REFERENCES watch_items(item_id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

---

## 3. 백엔드 아키텍처 및 REST API 명세 (Spring Boot)

### 3.1. API Endpoints (Prefix: /api/v1)
- POST /auth/signup : 회원가입 (email, password, nickname)
- POST /auth/login : 로그인 (JWT 발급)
- POST /auth/verify-phone : 판매 권한 획득용 휴대폰 인증
- GET /users/me : 내 프로필 및 등록 잔여 쿼터 조회
- GET /users/{userId}/profile : 판매자 프로필 및 거래 완료 내역 조회
- GET /items : 매물 목록 페이징 (Params: tier, brand, status, keyword, page, size, sort)
- GET /items/{itemId} : 매물 상세 조회 (view_count +1)
- POST /items : 신규 매물 등록 (JWT 필수, 휴대폰 인증 유저 전용, 일일 3건 쿼터 검증)
- PUT /items/{itemId} : 매물 정보 수정 (작성자 검증)
- PATCH /items/{itemId}/status : 거래 상태 변경 (FOR_SALE, RESERVED, SOLD)
- DELETE /items/{itemId} : 매물 삭제 (SOLD 매물은 400 Bad Request 예외 반환)
- POST /media/upload : 다중 이미지 파일 업로드 (S3 URL 배열 반환)

### 3.2. 매물 등록 요청 페이로드 규격
{
  "categoryTier": "HIGH_END",
  "brand": "ROLEX",
  "modelName": "서브마리너 데이트",
  "refNumber": "126610LN",
  "movementType": "AUTOMATIC",
  "caseSizeMm": 41.0,
  "dialColor": "블랙",
  "stampingDate": "2023-08",
  "originType": "DOMESTIC_STORE",
  "hasBox": true,
  "hasGuaranteeCard": true,
  "hasManual": true,
  "extraLinksCount": 2,
  "price": 17200000,
  "tradeType": "DIRECT_ONLY",
  "preferredLocation": "서울 강남구 압구정동 인근",
  "description": "2023년 백화점 스탬핑 풀세트입니다. 타임그래퍼 오차 정상이며 은행 직거래 희망합니다.",
  "images": [
    {
      "imageUrl": "[https://storage.watch-p2p.com/sample1.jpg](https://storage.watch-p2p.com/sample1.jpg)",
      "isRepresentative": true,
      "isVerificationPhoto": true,
      "sortOrder": 1
    }
  ]
}

### 3.3. 백엔드 비즈니스 로직 요구사항
1. ItemService.createItem()
   - seller.isPhoneVerified == true 검증 (미인증 시 403 Forbidden).
   - countBySellerIdAndCreatedAtAfter(24시간) >= 3 검증 (초과 시 429 Too Many Requests).
   - categoryTier == 'HIGH_END'인 경우 images 목록 내 isVerificationPhoto == true가 최소 1장 필수 (없을 시 400 Bad Request).
2. ItemService.deleteItem()
   - item.sellerId == requesterId 검증 (불일치 시 403 Forbidden).
   - item.itemStatus == 'SOLD'인 경우 "판매 완료된 매물은 시세 아카이브 정책에 따라 삭제할 수 없습니다" 예외 발생.

---

## 4. 프론트엔드 UI/UX 요구사항 (React + TypeScript)

### 4.1. 화면 구조 및 페이지
1. HomePage / ItemListPage
   - 상단: 통신판매중개자 법적 면책 배너 (상시 노출).
   - 2-Track 탭: [전체보기] | [엔트리·미드 관] | [하이엔드 관].
   - 필터바: 브랜드 셀렉터, 거래 가능만 보기 토글, 가격순/최신순 정렬.
   - 그리드 리스트: ItemCard 컴포넌트 렌더링.
2. ItemDetailPage
   - 이미지 갤러리 (실물 인증샷 뱃지 별도 표기).
   - 시계 메타데이터 규격 테이블 (브랜드, 모델, Ref, 구동방식, 직경, 다이얼, 스탬핑, 부속품 체크박스).
   - 판매자 정보 박스 (매너 점수, 닉네임, 완료 거래 건수).
   - 직거래 안심 수칙 안내 및 연락처/채팅 연동 버튼.
3. ItemCreatePage
   - 카테고리 선택 (1,000만 원 기준 라우팅 가이드 제공).
   - 정형화된 제원 입력 폼 (텍스트 서술 지양, 셀렉트/체크박스 위주).
   - 이미지 업로더 (하이엔드 선택 시 인증 메모 사진 지정 필수화).

### 4.2. UI 상태 표현 룰
- itemStatus === 'SOLD':
  - 카드 썸네일 중앙에 반투명 오버레이와 함께 'SOLD OUT' 라벨 표시.
  - 카드 전체 투명도(opacity: 0.6) 적용.
  - 상세 화면에서도 거래 완료 상태 유지 및 가격 데이터 보존.
- categoryTier 뱃지:
  - HIGH_END: 앰버/골드 계열 뱃지.
  - ENTRY_MID: 슬레이트/블루 계열 뱃지.